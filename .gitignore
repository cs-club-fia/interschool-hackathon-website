node_modules/
.wrangler/
.dev.vars
dist/
*.log
.DS_Store

# Plaintext login input for build-logins.mjs -- NEVER commit real passwords
logins.input.json

# Local secrets for the Piston tunnel scripts (auth tokens, worker URL)
scripts/piston.local.json
